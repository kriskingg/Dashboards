# Dashboard System Architecture

This document describes the cross-dashboard topology discovered during ownership mapping. It intentionally separates **generator ownership**, **serving ownership**, and **URL/ingress ownership**.

## Core principle

A dashboard has at least three potentially different owners:

1. **Generator** — code that builds the HTML/data/UI.
2. **Serving layer** — process that serves the generated page over HTTP.
3. **Ingress** — Cloudflare/Tailscale/reverse proxy that exposes the service.

Do not collapse these into one concept.

## Current known surfaces

```text
Cloudflare quick tunnel
triumph-events-chair-problems.trycloudflare.com
             |
             v
      127.0.0.1:8765
             |
             v
nifty-multi-shadow-dashboard.service
             |
             v
   mcx_paper.control_server
             |
             v
/var/lib/chartink-paper/nifty-multi-shadow/reports
             |
             +-----------------------------+
             |                             |
             v                             v
     live_latest.html               mcx_latest.html
     NIFTY research                 MCX / Silver research
             ^                             ^
             |                             |
             |                             |
/opt/chartink-paper/              /opt/hedge-engine
nifty-options-v2                  hedge_engine.runtime.paper_loop
analysis/nifty_multi_shadow/               |
live.py + dashboard.py                     +--> dashboard/renderer.py
             |                              +--> dashboard/renderer_core.py
             v                             |
GitHub:                                    v
kriskingg/etf-invest-engine        GitHub:
branch: chatgpt/                   kriskingg/hedge-engine
statistical-options-buying-        branch: main
basket-v1
```

This common serving layer is the main source of historical confusion. The two pages have different generator owners.

## Separate ETF/Tailscale surfaces

Known URLs awaiting independent ownership tracing:

```text
https://etf-trader.tailabfd53.ts.net/
https://etf-trader.tailabfd53.ts.net/v2/
```

These URLs must not be assumed to map to the same runtime, repo, branch, or service as either NIFTY or MCX until proven.

## D01 generator architecture

The NIFTY dashboard is generated by the NIFTY multi-shadow runtime:

```text
nifty-multi-shadow-live.service
          |
          v
/opt/chartink-paper/nifty-options-v2/
analysis/nifty_multi_shadow/oci/run_live.sh
          |
          v
analysis/nifty_multi_shadow/live.py
          |
          +--> dashboard.py
          |
          v
/var/lib/chartink-paper/nifty-multi-shadow/reports/live_latest.html
```

The deployed top-level `analysis/` runtime files are currently untracked copies. Their hashes matched the authoritative tracked files under:

```text
nifty-options-paper-research/analysis/nifty_multi_shadow/
```

on the active GitHub branch at verification time.

## D02 generator architecture

The MCX / Silver dashboard source is independently owned by:

```text
kriskingg/hedge-engine
branch: main
```

The deployment contract is:

```text
hedge-engine-paper.service
          |
          v
/opt/hedge-engine
python -m hedge_engine.runtime.paper_loop
          |
          +--> hedge_engine/dashboard/renderer.py
          +--> hedge_engine/dashboard/renderer_core.py
          |
          +--> primary report:
          |    /var/lib/hedge-engine/reports/mcx_latest.html
          |
          +--> published shared copy:
               /var/lib/chartink-paper/nifty-multi-shadow/reports/mcx_latest.html
```

The GitHub source was verified on 2026-09-19. The exact current OCI deployment branch/HEAD and byte-for-byte equivalence to the latest `main` branch were not re-verified in that GitHub-only review.

## Serving architecture

The serving layer currently comes from:

```text
/opt/chartink-paper/mcx-paper-research
```

and runs:

```text
python -m mcx_paper.control_server
```

against:

```text
/var/lib/chartink-paper/nifty-multi-shadow/reports
```

This means a page can be **served by MCX infrastructure without being generated by the same MCX deployment tree**.

For D01, the NIFTY page is generated by `etf-invest-engine` code and only served through the shared control server.

For D02, the MCX page is generated by `hedge-engine` and then copied/published into that same shared report directory.

## Git/repository architecture

A repository name is not an application boundary.

The current known generator ownership is:

```text
D01 NIFTY paper research -> kriskingg/etf-invest-engine
D02 MCX / Silver hedge   -> kriskingg/hedge-engine
```

`kriskingg/Dashboards` is the ownership/evidence registry, not the executable owner of either application.

`kriskingg/etf-invest-engine` contains or historically contained multiple responsibilities, including:

- ETF/live execution code;
- market-data/research code;
- NIFTY/BANKNIFTY paper/research implementations;
- deployment definitions;
- historical/research branches.

Therefore every dashboard record must identify the **branch and source path**, not merely the repository.

## Deployment-state architecture

Runtime state and generated reports live outside the Git source tree:

```text
/var/lib/chartink-paper/
/var/lib/hedge-engine/
```

This is desirable for generated/runtime data, but the current D01 deployment also uses an untracked runtime code copy at the repository top level. That creates deployment drift risk.

Until a dedicated cleanup/migration is designed, treat the current server layout as operational state and do not destructively normalize it.

## Identity rule for future work

When a user refers to a dashboard, identify it using this tuple:

```text
Dashboard ID + URL + server + generator module + source repository
```

Examples:

```text
D01
URL: triumph.../live_latest.html
Server: chartink-paper
Generator: nifty_multi_shadow/live.py
Source: kriskingg/etf-invest-engine

D02
URL: triumph.../mcx_latest.html
Server: chartink-paper
Generator: hedge_engine.runtime.paper_loop
Source: kriskingg/hedge-engine
```

Do not use labels like "ETF dashboard", "paper dashboard", or "the Cloudflare page" alone when multiple systems are in scope.
